# > Dumping some `mostly` useful shell scripts here since 2026
(with the exception of the immagged repo, which does not solely consist of a script file).

Note to self: run the cp command with the corresponding parameters whenever necessary to hard link new files into this repo.